<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full fixed bg-white top-0 left-0 px-10 py-5 flex justify-between ">
        <h3 class="text-xl">Scratch</h3>
        <% var isLoggedin = typeof isLoggedin !== "undefined" ? isLoggedin : true %>
        <% if(isLoggedin){ %>
            <div class="flex gap-7">
                <a href="/shop">Shop</a>
                <a href="">Account</a>
                <a href="">Cart</a>
                <a class="text-red-600" href="/users/logout">Logout</a>
            </div>
        <% } %>      
    </nav>